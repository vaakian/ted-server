{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "functions/index.js",
  "scripts": {
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && netlify-lambda serve src",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && netlify-lambda build src",
    "serve": "node local.js"
  },
  "keywords": [],
  "type": "commonjs",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "node-fetch": "2"
  },
  "devDependencies": {
    "serverless-http": "^3.0.1"
  }
}
